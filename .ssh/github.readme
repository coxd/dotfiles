- Generate the key: 

    `ssh-keygen -t ed25519 -C "github" -f ~/.ssh/id_ed25519_github`

- Add to ssh-agent and store password in keychain: 

    `ssh-add --apple-use-keychain ~/.ssh/id_ed25519_github`

- Find password in keychain: 

    Search for "id_ed25519_github" in keychain.

Cannot have homebrew version of ssh in path for any of this.